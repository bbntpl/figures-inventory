extends layout

block content
	h1(class='text-2xl font-bold mb-4') Character Detail

	p Name: #{character.name}
	p Franchise: #{character.franchise}
	if character.background
		p Background: #{character.background}